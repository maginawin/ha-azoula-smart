name: Release

on:
  release:
    types: [ "published" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release-zip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: ZIP Component Dir
        run: |
          cd ${{ github.workspace }}/custom_components/azoula_smart
          zip -r azoula_smart.zip ./

      - name: Upload zip to release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ${{ github.workspace }}/custom_components/azoula_smart/azoula_smart.zip
          asset_name: azoula_smart.zip
          tag: ${{ github.ref }}
          overwrite: true
